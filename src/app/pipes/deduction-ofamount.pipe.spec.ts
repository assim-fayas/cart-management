import { DeductionOFAmountPipe } from './deduction-ofamount.pipe';

describe('DeductionOFAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new DeductionOFAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
